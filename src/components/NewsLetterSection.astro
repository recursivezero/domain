<section class="bg-background text-foreground">
  <div class="mx-auto max-w-7xl px-6 py-14 lg:px-10 lg:py-16">
    <div
      class="border-foreground/10 bg-background/60 mx-auto max-w-3xl rounded-3xl border px-6 py-10 text-center backdrop-blur-xl lg:px-10"
    >
      <!-- Label -->
      <p class="mb-3 text-xs font-semibold uppercase tracking-[0.25em] text-blue-600">Stay Updated</p>

      <!-- Heading -->
      <h2 class="text-foreground text-3xl font-bold tracking-tight md:text-4xl">Subscribe to our newsletter</h2>

      <!-- Description -->
      <p class="text-foreground/70 mx-auto mt-4 max-w-xl text-base leading-7">
        Receive latest updates, blogs and important announcements directly in your inbox.
      </p>

      <!-- Form -->
      <form class="mx-auto mt-8 max-w-2xl">
        <div class="flex flex-col gap-3 sm:flex-row">
          <!-- INPUT FIXED -->
          <input
            type="email"
            placeholder="Enter your email address"
            class="border-foreground/10 dark:bg-background text-foreground placeholder:text-foreground/40 h-12 w-full rounded-xl border bg-white px-4 outline-none focus:border-cyan-500"
          />

          <!-- BUTTON -->
          <button
            type="submit"
            class="bg-brandBlue h-12 rounded-xl px-7 font-semibold text-white transition hover:scale-105"
          >
            Subscribe
          </button>
        </div>
      </form>

      <!-- Bottom Note -->
      <p class="text-foreground/50 mt-4 text-sm">No spam. Only quality updates.</p>
    </div>
  </div>
</section>
